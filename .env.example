# ClassFinal 环境变量配置示例
# 复制此文件为 .env 并根据需要修改

# Maven 构建配置
# 是否使用国内镜像源（阿里云）加速构建
# 本地开发建议设置为 true，CI 环境设置为 false
# USE_CHINA_MIRROR=true

# ClassFinal 运行配置
# 加密密码（可选）
# PASSWORD=your_password_here

# 要加密的包名（多个包用逗号分隔）
# PACKAGES=com.example.package1,com.example.package2

# 输入文件路径
# INPUT_FILE=/path/to/input.jar

# 输出文件路径（可选，默认为输入文件名-encrypted.jar）
# OUTPUT_FILE=/path/to/output-encrypted.jar

# JavaAgent 模式下的目标 JAR 文件
# TARGET_JAR=/path/to/target.jar
